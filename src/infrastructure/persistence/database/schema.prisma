datasource db {
  provider = "mysql"
  url      = "mysql://root@localhost:3306/db_crud"
}

generator client {
  provider = "prisma-client-js"
}

model users {
  id        Int    @id @default(autoincrement())
  name      String
  email     String @unique
}
